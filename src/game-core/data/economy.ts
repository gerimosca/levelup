/**
 * Constantes de "economía de la vida real" para estadísticas (configurables).
 * Ver docs/design/02-game-design.md §15.
 */

/** Valores por defecto del cálculo de dinero ahorrado (el usuario los edita). */
export const DEFAULT_BEER_PRICE = 2.5; // € por cerveza
export const DEFAULT_BEERS_PER_DAY = 3;

/** XP de bonus por escribir el diario nocturno (una vez al día). */
export const JOURNAL_XP_BONUS = 15;
